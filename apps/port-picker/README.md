# Port picker

**Backlog:** T-100 · `port-picker`

Superseded — folded into [`addons/ports`](../../addons/ports) (list listeners + kill by pid/port; also absorbs `port-scan-local` and the common-ports-bar gap). This stub is kept only as a pointer.
