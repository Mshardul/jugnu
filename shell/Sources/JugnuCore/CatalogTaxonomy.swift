import Foundation

// the only source of catalog category/subcategory/tag values — UI and registry copy must match
public enum CatalogTaxonomy {
    public static let categories = ["Clipboard", "Focus", "System", "Info", "Notes"]
    public static let tags = ["quick-glance", "toggle", "background", "popup-ui", "dev-tool", "recommended"]
}
