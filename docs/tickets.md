# Tickets

Lightweight future-work tracker — one row per ticket, not a full spec. A ticket captures intent, why it was split out, and any concrete findings that seeded it, so nothing gets lost between now and whenever it's picked up.

When a ticket is ready to start: brainstorm → write a real spec under [`docs/architecture/`](architecture/README.md) → mark this row **Done** with a link to that spec in Remarks.

| Ticket ID | Summary | Description | Priority | Effort | Depends on | Status | Created on | Last updated on | Remarks |
|---|---|---|---|---|---|---|---|---|---|
| 0001 | Persistent invoke latency logging | Every command invoke writes a structured JSON-lines record (addon id, command id, phase timings, pass/fail vs. budget) to a local file, day-capped retention, so a user can later volunteer it for diagnosis. Timing/ids only — never clipboard text or command args; privacy is a hard constraint, not a nice-to-have. | Medium | M | — | Not started | 2026-08-23 | 2026-08-23 | Split from [2026-08-23 product pass](architecture/2026-08-23-palette-ui-product-pass.md) |
| 0002 | Addon catalog browse (taxonomy + install) | Registry `category`/`subcategory`/`tags`/`description`/`commands`; install/uninstall/enable/disable; registry cache + offline fallback; `scripts/sync-registry-commands.py` via pre-commit + CI. Taxonomy/install already shipped. Chrome is not this ticket — in-panel `catalog`/`detail` is [0008](tickets.md). Do not add work on titled `BrowseCatalogWindow`. | Medium | L | 0001's epic's `JugnuUI` tokens/panels landed | In progress — taxonomy/install shipped; chrome moves to 0008 | 2026-08-23 | 2026-08-23 | Spec: [2026-08-23 addon catalog browse](architecture/2026-08-23-addon-catalog-browse-design.md). Chrome: [0008](tickets.md). Follow-on: 0005, 0006, 0007. |
| 0003 | Security audit — installer/runner/registry-trust hardening | Dedicated security pass, not a bolt-on fix. Seeded finding: `AddonInstaller.unzip()` (`shell/Sources/JugnuCore/AddonInstaller.swift`) shells out to `/usr/bin/unzip` with no zip-slip/path-traversal protection on extracted entries. Also scope: `AddonRunner` process-spawning safety, and the sha256-only (no code signing) registry trust model already flagged as a v0 non-goal in shell-design.md. | High | M | — | Not started | 2026-08-23 | 2026-08-23 | Split from [2026-08-23 product pass](architecture/2026-08-23-palette-ui-product-pass.md) |
| 0004 | First-run redesign — full catalog + recommended tag | `FirstRunWindow` moves from installing a fixed curated-5 set to showing the full registry catalog with `recommended`-tagged addons pre-selected, plus Skip/Next flow; final step redirects into Browse Catalog so the user can see what installed and explore more. Split out of ticket 0002's spec discussion — reuses that epic's registry/tags but lands in 0008’s `catalog` preset, not a titled `BrowseCatalogWindow`. | Medium | M | 0002 (registry tags), 0008 (`catalog` preset) | Not started | 2026-08-23 | 2026-08-23 | Split from [2026-08-23 addon catalog browse](architecture/2026-08-23-addon-catalog-browse-design.md) discussion |
| 0005 | Launcher: shell commands as real rows; compact when empty | Browse Addons and Preferences are searchable shell-native commands (keyboard, highlight, Return), not chrome outside the list. Do not mount an empty results `List` that fills the panel. When `first_view` is blank or recents/favorites are empty, `launcher` uses the compact size; it grows when there are rows. Yaml hide is [0012](tickets.md). | High | S | 0008 | Done | 2026-08-23 | 2026-08-23 | Landed in Task 8 of [shell-surface-presets plan](superpowers/plans/2026-08-23-shell-surface-presets.md); `PalettePanelController.swift` deleted (Task 5) |
| 0006 | Catalog state, errors, gestures, reload | Cards don’t refresh on enable/disable/uninstall (view model doesn’t observe `AppModel`). Install/enable errors are a window-level `PanelErrorBanner`, never cleared on success; per-card `errorMessage` unused. Card `onTapGesture` covers action buttons. Reopen doesn’t `load()`. No empty-filter copy. Must hold on 0008 in-panel `catalog`/`detail`. Uninstall **pushes** `confirm` on the stack; do not `dismissActive()` the host. | High | M | 0008 | Not started | 2026-08-23 | 2026-08-23 | Smoke 2026-08-23. Out of scope: AND tags (intentional), Disable/Uninstall `theme.error` (spec). |
| 0007 | Catalog / detail visual pass | Unthemed system-blue sidebar vs Firefly content; card vs page contrast too low; tag chips same fill as card; chips don’t wrap; system search field; Install not a primary control; flat type hierarchy; action rows not baseline-aligned. Do this on the in-panel `catalog`/`detail` presets (0008), not by polishing the titled `BrowseCatalogWindow`. | Medium | M | 0008 | Not started | 2026-08-23 | 2026-08-23 | Smoke 2026-08-23 |
| 0008 | Shell surface: one panel, presets, stack | One `KeyablePanel`. Navigation follows the tree from `launcher` (push child, replace sibling, pop on Esc/Cancel/Cmd+W/finish). Invoke hotkey / Open Palette: not on launcher → **home** (fresh `launcher`); on launcher → **close**. Click-outside dismisses (not Cmd+Tab). Compact launcher when empty; in-panel `catalog`/`settings`/`detail`/`list`/`form`/`confirm` with a morphing transition (Reduce Motion snaps). Toast stays a Jugnu-branded, themed HUD (not a stack node). `note` stays detached; host supports persist true/false. Settings includes a theme preview. Shell-native Browse Addons / Preferences commands. Long jobs wait on the current view; leave or quit cancels and runs cleanup (progress queue is later). Leaves `openCatalog` / `openSettings` for 0004. Supersedes 0002 titled Browse/Prefs windows. Glass later. | High | L | 0002 (registry/taxonomy/install) | Done | 2026-08-23 | 2026-08-23 | All 15 tasks landed: [shell-surface-presets plan](superpowers/plans/2026-08-23-shell-surface-presets.md). `UIHostController`/`PalettePanelController`/`BrowseCatalogWindowController`/`SkeletonPanel` deleted; zero stray references confirmed by repo-wide grep (Task 15). `openCatalog`/`openSettings` doors left as-is for 0004 (no concrete initial-state need yet — see Task 14 notes). Manual UI smoke (docs/architecture/shell-smoke.md) still pending — not run in this pass. |
| 0009 | Invoke hotkey keys the panel on the first press | First press of `shell.hotkey` must show the panel and focus search (not a second press). Same family as the old “hotkey fired, panel wasn’t key” bug. Must hold for 0008 home-or-close; ship in the 0008 PR unless this lands first. | High | S | — | Done | 2026-08-23 | 2026-08-23 | Landed in Task 8 of [shell-surface-presets plan](superpowers/plans/2026-08-23-shell-surface-presets.md) alongside home-or-close |
| 0010 | Catalog tag chips follow the current category | Every category/subcategory page shows the full tag vocabulary, including tags no addon in that set has. Chips should only list tags present in the current (filtered) set. | Medium | S | 0008 (`catalog` preset) | Not started | 2026-08-23 | 2026-08-23 | Smoke item 21, 2026-08-23 |
| 0011 | Recent records regardless of `first_view` | `AppModel.run` only calls `recordRecent` when `palette.first_view` is already `.recent`. Switching the picker to Recent after using Blank shows nothing. Record on run always; the picker only chooses what empty search displays. | Medium | S | — | Done | 2026-08-23 | 2026-08-23 | Fixed in Task 14 of [shell-surface-presets plan](superpowers/plans/2026-08-23-shell-surface-presets.md) — guard removed from `AppModel.runInvocation(for:)` |
| 0012 | Yaml gate for shell-native commands | Browse Addons and Preferences are shell-native commands (searchable, keyboard), mandatory in the UI, not My Addons toggles and not fake zips. Hidden only via `jugnu.yaml`. Depends on 0008. | Low | S | 0008 | Not started | 2026-08-23 | 2026-08-23 | Smoke item 18, 2026-08-23 |
| 0013 | Convention retrofit: names, comments, layering | Repo-wide pass so the tree matches [conventions](conventions.md): drop what-comments and “Task N” notes; rename `handleEsc` / `handleClickOutside` to outcome verbs; move `PrefsView` into `JugnuUI`. Do not clone those leftovers. Out of scope: lifetime/cancel (0014), SwiftFormat (0015). | Medium | M | — | Not started | 2026-08-23 | 2026-08-23 | Seeded from conventions review. `UIHostController` already deleted in 0008. |
| 0014 | Cancel in-flight work on leave | Spec: leave view or quit cancels the addon process and runs cleanup. Today `runInvocation` `Task.detached` is not cancelled from `ShellHost.hide()` / Esc / click-outside; catalog `install` `Task`s and palette `searchTask` are not cancelled on disappear. Also: monitor/Task retain cycles. Tests should fail if hide leaves a `Process` running. | High | M | 0008 | Not started | 2026-08-23 | 2026-08-23 | Seeded: `AddonRunner` + `AppModel.runInvocation`; `PaletteView` debounce Task; `BrowseCatalogView` install Task. Separate from 0013 (behavior, not naming). |
| 0015 | SwiftFormat as a commit/CI gate | After a dedicated format epic: pin SwiftFormat, format the tree once, then `swiftformat --lint` in pre-commit and CI (include `shell/App`). Do not land this as a drive-by in a feature PR — 0.62 currently wants a large wrap/comma/import rewrite. | Low | M | — | Not started | 2026-08-23 | 2026-08-23 | Parked from conventions hygiene discussion. Local `make format-swift` exists; not a hook. |
