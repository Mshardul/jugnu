# Tickets

Lightweight future-work tracker — one row per ticket, not a full spec. A ticket captures intent, why it was split out, and any concrete findings that seeded it, so nothing gets lost between now and whenever it's picked up.

When a ticket is ready to start: brainstorm → write a real spec under [`docs/architecture/`](architecture/README.md) → mark this row **Done** with a link to that spec in Remarks.

| Ticket ID | Summary | Description | Priority | Effort | Depends on | Status | Created on | Last updated on | Remarks |
|---|---|---|---|---|---|---|---|---|---|
| 0001 | Persistent invoke latency logging | Every command invoke writes a structured JSON-lines record (addon id, command id, phase timings, pass/fail vs. budget) to a local file, day-capped retention, so a user can later volunteer it for diagnosis. Timing/ids only — never clipboard text or command args; privacy is a hard constraint, not a nice-to-have. | Medium | M | — | Not started | 2026-08-23 | 2026-08-23 | Split from [2026-08-23 product pass](architecture/2026-08-23-palette-ui-product-pass.md) |
| 0002 | Addon catalog browse (taxonomy + install) | Registry `category`/`subcategory`/`tags`/`description`/`commands`; install/uninstall/enable/disable; registry cache + offline fallback; `scripts/sync-registry-commands.py` via pre-commit + CI. Taxonomy/install already shipped. Chrome is not this ticket — in-panel `catalog`/`detail` is [0008](tickets.md). Do not add work on titled `BrowseCatalogWindow`. | Medium | L | 0001's epic's `JugnuUI` tokens/panels landed | In progress | 2026-08-23 | 2026-08-29 | Taxonomy/install shipped; chrome moves to [0008](tickets.md). Spec: [2026-08-23 addon catalog browse](architecture/2026-08-23-addon-catalog-browse-design.md). Follow-on: 0005, 0006, 0007. |
| 0004 | First-run redesign — full catalog + recommended tag | `FirstRunWindow` moves from installing a fixed curated-5 set to showing the full registry catalog with `recommended`-tagged addons pre-selected, plus Skip/Next flow; final step redirects into Browse Catalog so the user can see what installed and explore more. Split out of ticket 0002's spec discussion — reuses that epic's registry/tags but lands in 0008’s `catalog` preset, not a titled `BrowseCatalogWindow`. | Medium | M | 0002 (registry tags), 0008 (`catalog` preset) | Not started | 2026-08-23 | 2026-08-29 | Split from [2026-08-23 addon catalog browse](architecture/2026-08-23-addon-catalog-browse-design.md) discussion. **Needs architecture discussion** — not bounded: wizard surface still undesigned (own window vs panel-hosted onboarding view), the 0825 launcher-catalog design routed around it, and the redirect must hook that design's viewB scopes/tags. Onboarding-tour questions in [menubar-design](architecture/2026-08-26-menubar-design.md) and [tools-launcher-design](architecture/2026-08-26-tools-launcher-design.md) also point here. Own spec required before a plan. |
| 0005 | Launcher: shell commands as real rows; compact when empty | Browse Addons and Preferences are searchable shell-native commands (keyboard, highlight, Return), not chrome outside the list. Do not mount an empty results `List` that fills the panel. When `first_view` is blank or recents/favorites are empty, `launcher` uses the compact size; it grows when there are rows. Yaml hide is [0012](tickets.md). | High | S | 0008 | Done | 2026-08-23 | 2026-08-23 | Landed in Task 8 of [shell-surface-presets plan](superpowers/plans/2026-08-23-shell-surface-presets.md); `PalettePanelController.swift` deleted (Task 5). Note: only shipped a header button, not searchable command rows — the "real rows, keyboard, Return" part landed later with [0012](tickets.md) 2026-08-29. |
| 0006 | Catalog state, errors, gestures, reload | Cards don’t refresh on enable/disable/uninstall (view model doesn’t observe `AppModel`). Install/enable errors are a window-level `PanelErrorBanner`, never cleared on success; per-card `errorMessage` unused. Card `onTapGesture` covers action buttons. Reopen doesn’t `load()`. No empty-filter copy. Must hold on 0008 in-panel `catalog`/`detail`. Uninstall **pushes** `confirm` on the stack; do not `dismissActive()` the host. | High | M | 0008 | Done | 2026-08-23 | 2026-08-23 | Fixed: `refreshTick` published counter in `BrowseCatalogViewModel` bumped after install/setEnabled/uninstall so cards redraw with live `isInstalled`/`isEnabled`; `errorMessage` cleared on success in all three; card tap moved off the whole card onto a `.plain` `Button` wrapping only name/summary/tags so `AddonActionRow` buttons no longer trigger navigation; empty-filter copy added to `BrowseCatalogView`. Reopen-doesn't-`load()` verified NOT a bug — `ShellHost.setContent` builds a fresh `NSHostingView` each push so `.task` reruns `load()` on every reopen. Out of scope: AND tags (intentional), Disable/Uninstall `theme.error` (spec). Build + full `swift test` (82 tests) green. Manual shell-smoke.md catalog walk not run this pass. |
| 0007 | Catalog / detail visual pass | Unthemed system-blue sidebar vs Firefly content; card vs page contrast too low; tag chips same fill as card; chips don’t wrap; system search field; Install not a primary control; flat type hierarchy; action rows not baseline-aligned. Do this on the in-panel `catalog`/`detail` presets (0008), not by polishing the titled `BrowseCatalogWindow`. | Medium | M | 0008 | Not started | 2026-08-23 | 2026-08-23 | Smoke 2026-08-23 |
| 0008 | Shell surface: one panel, presets, stack | One `KeyablePanel`. Navigation follows the tree from `launcher` (push child, replace sibling, pop on Esc/Cancel/Cmd+W/finish). Invoke hotkey / Open Palette: not on launcher → **home** (fresh `launcher`); on launcher → **close**. Click-outside dismisses (not Cmd+Tab). Compact launcher when empty; in-panel `catalog`/`settings`/`detail`/`list`/`form`/`confirm` with a morphing transition (Reduce Motion snaps). Toast stays a Jugnu-branded, themed HUD (not a stack node). `note` stays detached; host supports persist true/false. Settings includes a theme preview. Shell-native Browse Addons / Preferences commands. Long jobs wait on the current view; leave or quit cancels and runs cleanup (progress queue is later). Leaves `openCatalog` / `openSettings` for 0004. Supersedes 0002 titled Browse/Prefs windows. Glass later. | High | L | 0002 (registry/taxonomy/install) | Done | 2026-08-23 | 2026-08-23 | All 15 tasks landed: [shell-surface-presets plan](superpowers/plans/2026-08-23-shell-surface-presets.md). `UIHostController`/`PalettePanelController`/`BrowseCatalogWindowController`/`SkeletonPanel` deleted; zero stray references confirmed by repo-wide grep (Task 15). `openCatalog`/`openSettings` doors left as-is for 0004 (no concrete initial-state need yet — see Task 14 notes). Manual UI smoke (docs/architecture/shell-smoke.md) still pending — not run in this pass. |
| 0009 | Invoke hotkey keys the panel on the first press | First press of `shell.hotkey` must show the panel and focus search (not a second press). Same family as the old “hotkey fired, panel wasn’t key” bug. Must hold for 0008 home-or-close; ship in the 0008 PR unless this lands first. | High | S | — | Done | 2026-08-23 | 2026-08-23 | Landed in Task 8 of [shell-surface-presets plan](superpowers/plans/2026-08-23-shell-surface-presets.md) alongside home-or-close |
| 0010 | Catalog tag chips follow the current category | Every category/subcategory page shows the full tag vocabulary, including tags no addon in that set has. Chips should only list tags present in the current (filtered) set. | Medium | S | 0008 (`catalog` preset) | Done | 2026-08-23 | 2026-08-29 | Already landed with 0006: `availableTags()` in `BrowseCatalogFilter.swift` scopes to category/subcategory/search, `BrowseCatalogView.tagChips` renders only those. Verified 2026-08-29; `BrowseCatalogFilterTests` covers the category-scoping and tag-filter-ignore cases. |
| 0011 | Recent records regardless of `first_view` | `AppModel.run` only calls `recordRecent` when `palette.first_view` is already `.recent`. Switching the picker to Recent after using Blank shows nothing. Record on run always; the picker only chooses what empty search displays. | Medium | S | — | Done | 2026-08-23 | 2026-08-23 | Fixed in Task 14 of [shell-surface-presets plan](superpowers/plans/2026-08-23-shell-surface-presets.md) — guard removed from `AppModel.runInvocation(for:)` |
| 0012 | Yaml gate for shell-native commands | Browse Addons and Preferences are shell-native commands (searchable, keyboard), mandatory in the UI, not My Addons toggles and not fake zips. Hidden only via `jugnu.yaml`. Depends on 0008. | Low | S | 0008 | Done | 2026-08-23 | 2026-08-29 | New `ShellNativeCommand` (JugnuCore) with `visible(hidden:)` + fuzzy `matchScore`. `AppModel.shellNativeCommands` reads `shell.hidden_shell_commands`. `PaletteView` renders them below addon hits (empty query = both; typed = fuzzy-matched), unified up/down/Return via `LauncherSelection.resolve`; `JugnuApp.runShellNative` routes to `pushCatalog`/`pushSettings`. Old `⚙︎ All addons` header button removed. 8 tests; full `swift test` (157) green. 0005 marked Done early — this is the part it left unbuilt. Walk shell-smoke item 18. |
| 0013 | Convention retrofit: names, comments, layering | Repo-wide pass so the tree matches [conventions](conventions.md): drop `///` / `/* */` / what-comments / “Task N” notes (keep at most one `//` why); rename `handleEsc` / `handleClickOutside` to outcome verbs; move `PrefsView` into `JugnuUI`. Do not clone those leftovers. Out of scope: lifetime/cancel (0014), SwiftFormat (0015). | Medium | M | — | In progress — partial | 2026-08-23 | 2026-08-30 | Seeded from conventions review. `UIHostController` already deleted in 0008. **2026-08-30:** the `handleEsc` → `popOrDismiss` / `handleClickOutside` → `dismissFromClickOutside` renames landed in epic 0057 phase 2 (all call sites updated). `///` retrofit and `PrefsView` move still pending. |
| 0014 | Cancel in-flight work on leave | Spec: leave view or quit cancels the addon process and runs cleanup. Today `runInvocation` `Task.detached` is not cancelled from `ShellHost.hide()` / Esc / click-outside; catalog `install` `Task`s and palette `searchTask` are not cancelled on disappear. Also: monitor/Task retain cycles. Tests should fail if hide leaves a `Process` running. | High | M | 0008 | Done | 2026-08-23 | 2026-09-04 | Folded into epic [0057](tickets.md) — [addon process lifecycle](architecture/2026-08-30-addon-process-lifecycle-design.md) §3/§4/§9, phases 1–2. `oneshot` cancellation is covered (the awaiting `Task` is cancelled → `terminate()`, host entry + marker are the backstop) — the "hide leaves no `Process`" canary test targets exactly this. Seeded: `AddonRunner` + `AppModel.runInvocation`; `PaletteView` debounce Task; `BrowseCatalogView` install Task. Separate from 0013 (behavior, not naming). **2026-08-30:** phase 2 done — every dismiss edge (Esc / click-outside / pop / close) cancels the owned invoke `Task` + `host.killTracked`; `PaletteView.searchTask` and `BrowseCatalogView` install `Task`s cancel on disappear; `test_dismiss_leavesNoTrackedProcess` canary is green.  **2026-09-04:** closed with epic 0057 (phases 1–5 shipped).|
| 0015 | SwiftFormat as a commit/CI gate | After a dedicated format epic: pin SwiftFormat, format the tree once, then `swiftformat --lint` in pre-commit and CI (include `shell/App`). Do not land this as a drive-by in a feature PR — 0.62 currently wants a large wrap/comma/import rewrite. | Low | M | — | Not started | 2026-08-23 | 2026-08-23 | Parked from conventions hygiene discussion. Local `make format-swift` exists; not a hook. |
| 0016 | Keep KeyablePanel alive across hide | `ShellHost.hide()` sets `panel = nil`, so the next invoke recreates the NSPanel and recolds the 50 ms paint budget. Keep the panel, `orderOut` on dismiss, `orderFront` on invoke. Out of scope: 0014 cancel, 0015 format. | Medium | S | 0008 | Done | 2026-08-23 | 2026-08-29 | `hide()` no longer nils `panel`; `ensurePanel` stays a one-time build, `orderFront` reuses it. `hasPanel` test hook + two tests in `ShellHostFrameTests.swift`. Full `swift test` (149) green. Felt-speed re-check belongs in shell-smoke.md. |
| 0017 | App self-update (custom mechanism) | Jugnu.app has no update path today — a user drifts onto a stale build with no in-app signal or fetch. Ship a custom checker first (piggyback the existing sha256-verified registry pattern: a small `jugnu-app-registry.json` with latest version/sha256/download URL, checked on launch, user-prompted, manual download+swap). Sparkle is the real long-term answer but needs Developer ID signing + notarization first (Apple Developer Program membership is the actual blocker) — tracked separately, do not block this ticket on it. | High | M | — | Not started | 2026-08-23 | 2026-08-29 | Discussed 2026-08-23. Custom-now/Sparkle-later is deliberate — revisit once code signing is in place. **2026-08-29:** the code-signing dependency is now tracked by the recurring [security audit](audit/prompts/security.md), not a ticket (deleted 0003, which was an audit-shaped row). |
| 0018 | Addon upgrade-in-place | Registry has versions (`addons-v1.0.0`) but nothing flags an installed addon as outdated once a newer version ships. Catalog card shows an "update available" badge when installed version < registry version; update is user-triggered (manual by default), never automatic — matches the app's privacy/control stance. | Medium | M | 0002 (registry/taxonomy) | Done | 2026-08-23 | 2026-09-06 | Absorbed by [0058](tickets.md) Phase 5. Manual Update badge/action; preserve enabled; same install pipeline. |
| 0019 | Addon-error taxonomy + dev-only logs | Today a failed addon invoke likely surfaces one generic error via `PanelErrorBanner`. Users need to know **which addon** failed and **what happened**, in plain language (never a raw stderr/stack dump) — distinguish at least hang/timeout, non-zero exit, and malformed-stdout-JSON as different plain-language messages. Separately, log these events to a local dev-only file (distinct from 0001's latency-only `InvokeTrace` logging) — never clipboard content or command args, matching [Privacy and trust](../conventions.md#privacy-and-trust). 30-day retention, oldest entries pruned on launch (see 0038). No user-facing log viewer yet; that is future scope once a real UI is designed for it. | Medium | M | — | Not started | 2026-08-23 | 2026-08-30 | Discussed 2026-08-23, retention added 2026-08-24. Keep distinct from 0001 (timings/ids only) — this ticket's logs carry error content, so privacy review applies at write time, not just at display time. Hang/timeout *detection* is covered by epic [0057](architecture/2026-08-30-addon-process-lifecycle-design.md) §2 (per-class timeout); this ticket keeps the plain-language message + dev log, which coordinate with epic 0055. **0057 defines a `LifecycleEvent` record (kill / reap / safe-mode trip). It already ships an interim capped `lifecycle.log` (reap + safe-mode events, all build configs); this ticket's dev-log subsumes that file and adds the rest of the taxonomy.** Other lifecycle events stay `#if DEBUG` until then. |
| 0020 | Single-instance relaunch guard | `make run` today manually stops any already-running Jugnu before launching the new build — that's a dev-flow crutch, not app-level protection. Ship a real single-instance guard: on launch, detect an already-running Jugnu.app and activate/focus it (open the palette) instead of spawning a second instance that would fight over the hotkey and menu-bar slot. | High | S | — | Done | 2026-08-23 | 2026-08-29 | Done — pure `SingleInstance.shouldYield` + `AppDelegate.yieldToRunningInstance()` (posts `DistributedNotificationCenter` open-palette, then terminates before hotkey/menu-bar setup). `SingleInstanceTests`; `swift test` green. Manual double-launch check in shell-smoke.md. |
| 0021 | Addon sandboxing / per-addon permission model | Every addon today runs with the same privilege as the host app — fine at 11 first-party addons, becomes a real design question the moment addons aren't all written in-house (ties into a future open registry / addon dependencies, 0025). Scope: should `clipboard-history` see less than `mic-mute`? Needs its own design pass. | Low | L | — | Not started | 2026-08-23 | 2026-08-29 | Discussed 2026-08-23. Explicitly future — no concrete addon proposal needs this yet. **2026-08-29:** `Depends on` was 0003; that row was an audit-shaped ticket and got deleted. The installer/runner/trust groundwork this ticket wants is now the recurring [security audit](audit/prompts/security.md) — let it run before starting this. |
| 0022 | OS-upgrade / TCC permission-reset detection | macOS major upgrades routinely reset TCC (Accessibility/Input Monitoring) grants. Today's manual smoke checklist covers permission-denial by hand, but nothing **proactively detects** "Jugnu just lost Accessibility after an OS update" — a user would only notice because the hotkey silently stopped working. Add a launch-time or periodic permission check that surfaces a clear in-app prompt to re-grant, instead of relying on the user to notice and debug it themselves. | High | M | — | Not started | 2026-08-23 | 2026-08-23 | Discussed 2026-08-23. |
| 0023 | Disable vs uninstall — verify background agents actually stop | Confirm addon **disable** (still installed, just off) actually stops any background agent (e.g. `clipboard-history`'s `com.jugnu.clipboard-history.watch` launchd watcher) and not just hides the addon from the palette. If disable currently leaves a watcher running, that's a real privacy/resource bug, not a cosmetic one — the addon would keep observing the pasteboard while showing as "off." | High | S | — | Done | 2026-08-23 | 2026-08-29 | Confirmed bug: `performDisable` booted the label out but left `~/Library/LaunchAgents/<label>.plist`, so `KeepAlive` reloaded the watcher at next login. Fix: `performDisable` deletes the plist before bootout, verifies via `launchctl print`, throws `CleanupError.disableIncomplete` before the `setEnabled` config flip. `CleanupTests` + `swift test` green. Login-cycle check in shell-smoke.md. Re-enable / first enable bootstraps the watcher via shell `DaemonAgents` (ticket 0057 phase 5); `bin/run` no longer self-installs launchd. |
| 0024 | Full app uninstall with thorough cleanup | No documented or automated path to remove Jugnu.app itself cleanly. Ship a user-triggered "uninstall everything" option (menu bar or Preferences) that removes config (`~/.config/jugnu`), state (`~/.local/share/jugnu`), and every addon-declared `cleanup.launchd` LaunchAgent — not just addon-by-addon cleanup, the whole app. Privacy-first stance means this must be thorough, not best-effort. | Medium | M | — | Not started | 2026-08-23 | 2026-08-23 | Discussed 2026-08-23. "Thorough cleanup" is a hard requirement per the app's privacy stance, not a nice-to-have. |
| 0025 | Addon dependency declaration + install-time disclosure | Some addons will depend on **other catalog addons** (vision rule 4, user-would-install-alone case). Add a `dependencies` field to the addon manifest. At install time, show: "X depends on: [already installed/enabled ✓] [will be installed now]." Missing addons install as part of accepting the install; **enabling stays manual per addon** — nothing turns itself on silently. Shared **helpers** (not catalog products) are [0047](tickets.md), not this ticket. | Medium | M | 0002 (manifest/install) | Done | 2026-08-23 | 2026-09-06 | Absorbed by [0058](tickets.md) Phase 4. Exact SemVer deps, disclosure, transaction rollback. |
| 0026 | Per-addon invoke mode: reuse vs replace, enforced single-process | `AddonRunner` has no guard against re-invoking a command while a previous invoke of the same addon is still in flight. Add a manifest field (e.g. `invokeMode: reuse \| replace`) per addon: `reuse` re-invocation focuses/surfaces the already-running instance's UI instead of spawning a second; `replace` kills the prior process first, then spawns fresh. Either mode: **never** two concurrent processes for the same addon. | High | M | 0014 | Done | 2026-08-23 | 2026-09-04 | Folded into epic [0057](architecture/2026-08-30-addon-process-lifecycle-design.md) §7 (`on_reinvoke: reuse\|replace`, `job` only in-epic; the `(addon-id, command-id)`-keyed `AddonProcessHost` is the single-process guard, per-command not per-addon; `replace` = immediate SIGKILL + confirmed-death-before-respawn), phase 3.  **2026-09-04:** closed with epic 0057 (phases 1–5 shipped).|
| 0027 | In-app bug report / feedback path | No way to report a bug or send feedback from inside the app today — GitHub issues only, external. Add a lightweight "Report an Issue" entry (menu bar or Preferences) that opens a prefilled GitHub issue (or equivalent), consistent with the app having no other feedback loop. | Low | S | — | Not started | 2026-08-23 | 2026-08-23 | Discussed 2026-08-23. |
| 0028 | Catalog trust badge (first-party / verified) | Even single-publisher today, the catalog could show a "verified first-party" badge now, establishing visual precedent for trust tiers before any future open registry or addon-dependency (0025) story introduces addons from outside contributors. | Low | S | 0002 (catalog UI) | Not started | 2026-08-23 | 2026-08-23 | Discussed 2026-08-23. |
| 0029 | Universal binary (arm64 + x86_64) requirement for addons | Addon entrypoints `exec` native binaries; nothing in [addon-manifest.md](addon-manifest.md) states whether they must be universal (arm64+x86_64) or documents the real breakage risk on Intel Macs if an addon ships arm64-only. Document the requirement and add it to `scripts/validate-addon.sh`'s checks. | Medium | S | — | Done | 2026-08-23 | 2026-09-06 | Absorbed by [0058](tickets.md) Phase 3 (`PackageGates` + `validate-addon.sh`). |
| 0030 | Local anonymous usage log | No signal today about which commands actually get used, to guide roadmap. Build a structured, fully anonymous usage log (command/addon id + timestamp only — no clipboard/args/context, same privacy bar as [Privacy and trust](../conventions.md#privacy-and-trust)) that accumulates on the local machine only. 30-day retention, oldest entries pruned on launch (see 0038). No upload, no storage/analysis system, no opt-in prompt yet — that infra is explicitly future scope once there's a reason to build it. | Low | S | — | Not started | 2026-08-24 | 2026-08-24 | Discussed 2026-08-24, retention added 2026-08-24. Keep distinct from 0001 (latency/perf) and 0019 (errors) — this is usage-frequency only. |
| 0031 | Namespaced addon ids + collision resolution | Ids are unique by construction today (single trusted registry) — breaks the moment a second addon source exists (0025 dependencies, or any future non-first-party registry). Namespace ids (e.g. `jugnu.mic-mute` vs a third-party `<publisher>.mic-mute`). Collision resolution: first-party `jugnu.*` always wins over any other namespace; among non-`jugnu.*` collisions, whichever is installed first wins. Touches manifest, registry, `CommandIndex`, and every place ids are used — real migration for existing installed addons, not additive-only. | Low | M | 0025 | Done | 2026-08-24 | 2026-09-06 | Absorbed by [0058](tickets.md) Phase 6. Source + registry rewritten to `jugnu.*`; resumable `NamespaceMigrator` on launch. |
| 0032 | Warn before disabling/uninstalling an addon with an open UI panel | If an addon's panel is currently open (e.g. `floating-note`'s note window) and the user disables or uninstalls that addon from Preferences, today's behavior is undefined. Show an accept/reject alert before proceeding; accept force-closes the open panel and continues the disable/uninstall, reject cancels the action entirely. | Medium | S | — | Done | 2026-08-24 | 2026-09-04 | The disable-with-open-panel alert + force-close is folded into epic [0057](architecture/2026-08-30-addon-process-lifecycle-design.md) §9 (consumes `AddonProcessHost.hasTracked` / `killTracked`), phase 3. Uninstall-specific concerns + pairing with 0024 stay on this ticket. Note: with `session` cut from 0057 (→ epic 0059), the only tracked panels in-epic are `card` / `note` — full session-panel disable interaction is 0059.  **2026-09-04:** closed with epic 0057 (phases 1–5 shipped).|
| 0033 | Addon storage-usage view | `clipboard-history` (and any future full-history addon) can grow disk usage unbounded with no visibility. Add a view (Preferences, alongside the theme section) showing total and per-addon disk usage under `~/.local/share/jugnu/state/<id>`, so growth is visible before it's a surprise — not necessarily a hard cap in this ticket, just visibility first. | Medium | M | — | Not started | 2026-08-24 | 2026-08-24 | Discussed 2026-08-24. A hard quota/cap is a possible follow-on, not required here. |
| 0034 | Malformed `jugnu.yaml` recovery prompt | Theme hex-fallback (invalid value → default) is already covered, but a fully malformed YAML file (syntax error, not a bad value) has no defined behavior today. On load failure, show the user a clear message that the config file is malformed, with two actions: open it to fix manually, or reset to built-in defaults. | Medium | S | — | Done | 2026-08-24 | 2026-09-04 | Phase 4: YAML syntax errors take the shared recovery menu (reset / open / disable all / try again) instead of silently substituting defaults.
| 0035 | Flag `JUGNU_ADDON_PATH` dev override visibly in UI | One canonical install path (`~/.local/share/jugnu/addons/`) is the only place real, registry-verified addons should load from. `JUGNU_ADDON_PATH` (dev override, skips sha256/registry verification entirely) has no visual distinction from a normal session today — a leftover env var could silently load unverified code in daily use with no signal. Add a visible dev-mode banner/badge whenever the override is active; consider gating it behind a debug build entirely. | Medium | S | — | Not started | 2026-08-24 | 2026-08-24 | Discussed 2026-08-24. |
| 0036 | Crash-loop safe mode | No recovery path if the app crashes immediately on launch (bad config, bad theme, bad addon). Detect a crash loop (e.g. 2-3 consecutive launches crashing within a short window after start, via a launch-attempt counter written before risky startup work and cleared on a clean launch) and enter safe mode on the next launch: menu-bar-only, zero addons loaded, default theme forced, offering reset-config-to-defaults (0034) and/or disable-all-addons-and-relaunch as recovery actions. | High | M | 0034 | Done | 2026-08-24 | 2026-09-04 | Phase 4: 3 uncleared launch attempts trip menu-bar recovery, `com.jugnu.*` bootout, degraded reaper.
| 0037 | Hotkeys management view | Per-addon/per-command hotkeys have no single place to see or change them — only the shell's own Option+Space is covered (manual conflict checklist). Add a settings view listing every registered hotkey across shell + all addon commands on one page; user can rebind almost any of them, with collision detection at rebind time. | High | L | — | Not started | 2026-08-24 | 2026-08-24 | Discussed 2026-08-24. Real settings surface, not a bug-check — bigger than 0012/0016-scale tickets. |
| 0038 | Required-permissions disclosure on addon install card | Some addons need Accessibility/Input Monitoring/network; others need nothing. No pre-install disclosure exists today — the user only learns when macOS's own permission prompt fires mid-use. Add a required-permissions field to the addon manifest, surfaced on the catalog install card before the user installs (e.g. "This addon will need: Accessibility"). | High | M | 0002 (catalog UI) | Not started | 2026-08-24 | 2026-08-24 | Discussed 2026-08-24. Disclosure before install, not just at first permission prompt. |
| 0039 | Network-addon retry + no-connection error | Network-dependent addons (`weather-bar`, `brew-outdated`) have no defined offline behavior. On a failed network call, retry automatically a couple of times, then show a "no connection" error with two actions: Retry, Quit (dismiss that invoke — confirm exact wording/scope at spec time, not "quit the app"). | Medium | S | — | Not started | 2026-08-24 | 2026-08-24 | Discussed 2026-08-24. |
| 0040 | 30-day retention for local diagnostic logs | Dev-only error logs (0019) and anonymous usage logs (0030) have no stated retention policy — ironic for a privacy-focused app to let its own diagnostic logs accumulate unbounded. Bound both to 30 days; prune entries older than that on launch. | Medium | S | 0019, 0030 | Not started | 2026-08-24 | 2026-08-24 | Discussed 2026-08-24. Single shared pruning mechanism both 0019 and 0030 should use, not two separate implementations. |
| 0041 | No orphaned addon processes across sleep/wake | Some addons legitimately persist across sleep (e.g. a background watcher), declared per-addon (same shape as 0026's `invokeMode`). Any process **not** declared persistent must be cleanly terminated before or on sleep — never left as an orphan after wake. | High | M | 0026 | Done | 2026-08-24 | 2026-09-04 | Folded into epic [0057](architecture/2026-08-30-addon-process-lifecycle-design.md) §2 (`job` torn down on sleep; `daemon` = launchd, survives) + §6 (marker-file orphan reaper on wake as the crash-durable backstop). `session` sleep=leave is epic 0059. Phases 2–4.  **2026-09-04:** closed with epic 0057 (phases 1–5 shipped).|
| 0042 | Verify global hotkey behavior across fast user switching | Config/state/LaunchAgents are already per-user by construction (`~/.config`, `~/.local/share`, `~/Library/LaunchAgents` all per-home-directory) — no build needed there. Open question is whether macOS itself scopes a global hotkey (Option+Space) correctly per active session when two accounts are fast-user-switched on the same Mac, both running Jugnu. Verify and document; this is an OS-behavior question, not app code. | Low | S | — | Not started | 2026-08-24 | 2026-08-24 | Discussed 2026-08-24. Likely a documentation/verification task, not a fix. |
| 0043 | Addon manifest: minimum shell version | `api: 1` covers the JSON protocol version, but nothing lets an addon declare a floor on shell *internals* it depends on beyond that protocol (e.g. specific panel behavior). Add a `minShellVersion` (or similar) manifest field, checked at install/load time, separate from `api:`. | Medium | S | — | Done | 2026-08-24 | 2026-09-06 | Absorbed by [0058](tickets.md) Phase 3. |
| 0044 | Warn and kill in-flight addon processes on app quit | "Quit Jugnu" has no defined behavior for addon processes still running at quit time. Warn the user if any are in-flight, then terminate them on confirm. Distinct from 0014 (panel-leave cancels a process while the app keeps running) — this is full-app-quit scope. | High | S | 0014 | Done | 2026-08-24 | 2026-09-04 | Folded into epic [0057](architecture/2026-08-30-addon-process-lifecycle-design.md) §3/§9, phase 2. Quit prompts only for in-flight `job`; `killAll()` on the quit path is parallel + bounded (one shared `killGrace`, no per-child await — `applicationWillTerminate`'s ~5s OS deadline); pre-quit reaper sweep is the backstop. **2026-08-30:** phase 2 wired `applicationWillTerminate` + quit menu + willSleep → `host.killAll()`; `make stop` sends SIGTERM (bounded wait, then SIGKILL) so the quit path runs. The in-flight-`job` confirm prompt is deferred to phase 3 (no `job` class yet) — tracked here, not a code TODO.  **2026-09-04:** closed with epic 0057 (phases 1–5 shipped).|
| 0045 | Shell view types (viewport catalog) | Ten named view types (`seek`, `palette`, `ask`, `fields`, `rows`, `grid`, `board`, `spread`, `canvas`, `rail`). Shell sizes from current screen `visibleFrame` + point clamps. Addons allow-list ids; commands pick one. No per-addon pixels/%. Click-outside dismisses except `board`/`spread`/`canvas`. | High | M | 0008 | Done | 2026-08-24 | 2026-08-24 | Spec: [2026-08-24 view types](architecture/2026-08-24-view-types.md). ADR: [0002](architecture/decisions/0002-view-types.md). Plan: [2026-08-24-view-types](superpowers/plans/2026-08-24-view-types.md). Shell `ViewType` size table, click-outside split, manifest `view_types` / `view` / `ui.view`. |
| 0046 | `window-layouts` first-party addon | One zip: snaps + snap board + zones (max 6, replace picker, geometry only) + Space jump via system shortcuts. No undo. No scenes. AX-first compiled helper; SIP on; isolated CGS read/write. Accessibility on first use of this addon. | High | L | 0045 (`board`/`rows`/`fields`/`ask`) | Done | 2026-08-24 | 2026-08-24 | Spec: [2026-08-24 window-layouts](architecture/2026-08-24-window-layouts.md). Plan: [2026-08-24-window-layouts](superpowers/plans/2026-08-24-window-layouts.md). Folded layout-save into zones. Core tests + `validate-addon` + snap-board JSON verified. Catalog zip/sha on next addons release. Walk two-display / denied Accessibility / Stage Manager on a Mac. |
| 0047 | Shared helpers: download once, reuse | Vision rule 4. Rules locked in [addon manifest — Helpers](addon-manifest.md#helpers). **Shipped in Core:** `helpers:` on addon.yaml, `registry/helpers.json`, install-time fetch if missing, reuse on disk, `JUGNU_HELPER_*` env, last-consumer uninstall, plain offline error. No Play helper zip yet — catalog is empty `[]`. Bundles are [0048](tickets.md). | High | M | 0002 (install), 0025 (catalog-addon deps) | Done | 2026-08-24 | 2026-08-24 | Sequence: Play addons can now declare a helper. First consumer still needs a real helper package + registry row. |
| 0048 | Catalog bundles (multi-addon download) | Vision rule 5: a **bundle** is a one-click download of several catalog addons — not a zip, not an enable key, not a fourth catalog level. Uninstall/disable stay per addon. Play can ship as 14 separate addons until this exists. Needs its own product discussion (what a pack contains, how updates work, whether a bundle is visible in browse). Do not block helpers (0047) or individual Play zips. | Medium | L | 0002 (catalog/install) | Not started | 2026-08-24 | 2026-08-24 | Deferred 2026-08-24 as a separate epic. More design required before spec. |
| 0049 | `clock` helper + `nudges` addon | Shared timer helper + nudges mini-tool (user list, 3 presets, detached `card` UI, Shell ClockHost). Spec: [2026-08-25 nudges-clock-helper](architecture/2026-08-25-nudges-clock-helper-design.md). Plan: [2026-08-25-nudges-clock-helper](superpowers/plans/2026-08-25-nudges-clock-helper.md). Emoji only; PNG is [0050](tickets.md). | High | L | 0047 | Done | 2026-08-25 | 2026-08-25 | First real `registry/helpers.json` package. |
| 0050 | Nudges: custom PNG icons on cards | Optional PNG override per nudge row (Advanced/config path first; file picker later). SVG deferred. After emoji-only ship. | Medium | M | 0049 | Not started | 2026-08-25 | 2026-08-25 | Locked out of 0049 deliberately. |
| 0051 | Unified hand-drawn icon system — addon icons + UI chrome, theme-reactive (epic) | Grew past a one-row-with-findings ticket into a real epic — findings, open questions, and locked constraints (e.g. two-icon-per-state for stateful favorites, not glow/dim) now tracked in its own file: [2026-08-26 icon system design](architecture/2026-08-26-icon-system-design.md) (status: in progress, findings only, to be reviewed). Until picked up, use placeholders everywhere an icon appears — default app icon for addon icons, plain emoji/text glyphs for UI chrome (as in the current mockups). | Medium | L | — | In progress | 2026-08-25 | 2026-08-29 | Findings only, to be reviewed. Split from [2026-08-25 launcher + catalog browse design](architecture/2026-08-25-launcher-catalog-design.md) discussion. Scope expanded 2026-08-26 to include UI chrome icons. Promoted to epic 2026-08-26 with its own findings file. |
| 0052 | View types visual design (epic) | Ten-type viewport catalog ([view types](architecture/2026-08-24-view-types.md)) is locked as geometry only — most types (`ask`, `fields`, `rows`, `grid`, `board`, `spread`, `rail`, `toast`, `note`, `status`) have never had a real visual mockup, only `seek`/`palette`/`canvas` do (from the launcher/catalog design session). Findings, per-type addon assignment, and open questions tracked in [2026-08-26 view types visual design](architecture/2026-08-26-view-types-visual-design.md). Blocks finalizing 5 addons' view-type assignment (toast-only candidates: mic-mute, mute-all, focus-toggle, open-terminal-here, paste-plain). | Medium | L | — | In progress | 2026-08-26 | 2026-08-29 | Findings only, to be reviewed. Split from [2026-08-24 view types](architecture/2026-08-24-view-types.md) §7 per-addon audit discussion. |
| 0053 | Tools launcher (epic) | New icon-grid surface (Dock/homescreen-style), opened via a new button in viewA row1, giving non-technical users a visual way to run commands without needing the hotkey. Findings and open questions (favorites relationship, `grid` view type, content inventory) in [2026-08-26 tools launcher](architecture/2026-08-26-tools-launcher-design.md). | Medium | L | — | In progress | 2026-08-26 | 2026-08-29 | Findings only, to be reviewed. Split from launcher/catalog design discussion 2026-08-26. Kept separate from [0056](tickets.md) (menu bar) — related but distinct surfaces. |
| 0054 | Permissions, privacy & security (epic) | Everything touching permission requests, privacy disclosure, and security-relevant UI (install-time disclosure, runtime OS prompts, addon detail view's Permissions tab) gathered into one epic rather than designed piecemeal. Findings and open questions in [2026-08-26 permissions, privacy & security](architecture/2026-08-26-permissions-privacy-security-design.md). Coordinates with existing [ticket 0038](tickets.md) (pre-install disclosure). | Medium | L | 0038 | In progress | 2026-08-26 | 2026-08-29 | Findings only, to be reviewed. Split from launcher/catalog design discussion 2026-08-26. |
| 0055 | Error & failure states (epic) | Failure-path UI (install failure, offline registry fallback, hotkey conflict) whose behavior is already locked/tested but never visually designed — e.g. shared `PanelErrorBanner` component has a locked mechanism, no look. Findings and open questions in [2026-08-26 error & failure states](architecture/2026-08-26-error-failure-states-design.md). | Medium | M | — | In progress | 2026-08-26 | 2026-08-29 | Findings only, to be reviewed. Split from launcher/catalog design discussion 2026-08-26. |
| 0056 | Menu bar icon + dropdown (epic) | Menu bar icon (quit/palette/prefs today) has never had a real visual design, despite being the most discoverable entry point for non-technical first-time users. Findings and open questions in [2026-08-26 menu bar](architecture/2026-08-26-menubar-design.md). | Medium | M | — | In progress | 2026-08-26 | 2026-08-29 | Findings only, to be reviewed. Split from launcher/catalog design discussion 2026-08-26. Kept separate from [0053](tickets.md) (tools launcher) — related but distinct surfaces. |
| 0057 | Addon process lifecycle + crash recovery (epic) | The shell spawns addon children and forgets them — no `Process`/`Task` reference escapes `AddonRunner.run()`. Leave/Esc/quit/sleep/re-invoke all fail to stop a running addon; a startup crash or hang has no recovery; `disown`ed background work leaks unreapable. This epic makes the shell the owner of addon process lifetime: a **three-class** lifecycle model (`oneshot` / `job` / `daemon` — `session` is its own epic 0059), an `AddonProcessHost` (App-layer, `AppDelegate`-owned, keyed by `(addon-id, command-id)`), an `AddonRunner` spawn/wait split so cancellation reaches the OS process, **crash-durable per-spawn marker files** + an orphan reaper (launch/wake/quit), a `job` heartbeat watchdog, a first-party trust gate on `lifecycle: daemon`, and crash-loop safe mode. Designed for the final app (hundreds of third-party UI-first addons), not today's 19 fast-exit scripts. Full design: [2026-08-30 addon process lifecycle](architecture/2026-08-30-addon-process-lifecycle-design.md). | High | XL | — | Done | 2026-08-30 | 2026-09-04 | Spec written 2026-08-30, revised after review pass. Absorbs 0014, 0026, 0032 (open-panel-disable slice), 0034, 0036, 0041, 0044 and the hang/timeout-detection slice of 0019. **2026-09-04:** phases 4–5 landed (orphan reaper, crash-loop safe mode, malformed-yaml recovery; pomodoro via clock helper; keep-awake + clipboard-history shell-owned daemons). Committed phases 1–5 in §12; later phases in §10. `session` class cut → epic [0059](tickets.md).  Closed as **Done** 2026-09-04: plan phases 1–5 shipped. Spec [§10](architecture/2026-08-30-addon-process-lifecycle-design.md) deferred items (determinate job progress, daemon authoring, third-party daemon flag, startup watchdog) stay in that spec, not a reopen of this ticket. Manual shell-smoke 0057 checks still unwalked.|
| 0059 | `session` lifecycle class + addon IPC (epic) | Stub — a `session` is a live addon process bound to an open detached panel, exchanging messages while the panel is up (a game reacting to clicks, a chess-clock ticking on its own). Cut from [0057](architecture/2026-08-30-addon-process-lifecycle-design.md) (§11) because the class is only real with bidirectional IPC and shipping a class nothing can use is the speculative abstraction conventions.md forbids. This epic owns: the class definition + kill rules (killed on panel-leave/Esc, silently on quit, **sleep = leave**); `on_reinvoke: reuse` = focus panel / `replace` = end+restart; the **bidirectional IPC protocol** (newline-delimited JSON, addon-initiated frames, shell read-loop, `api: 2`); **stdout draining** before the read-loop exists; a **SIGTERM "about to be killed" flush hook** (interacts with 0057's `killGrace`); **multi-panel window management** (z-order, focus, per-panel vs shell-global click-outside, multi-display, which panel Esc closes); **session-UI teardown on quit/sleep** = `host.killTracked` + panel close. Most of the Play category (`chess-clock`, `breathing`, `reaction-time`, `memory`) is session-shaped; `tic-tac-toe` stays a re-invoke-per-move `oneshot` and does NOT need this. | Medium | XL | 0057 | Not started | 2026-08-30 | 2026-08-30 | Stub epic. No design doc yet. Gated on the first `session`-shaped addon. 0057's class enum is built open so adding `session` is a manifest-parse + handler change, not an architecture change. |
| 0058 | Addon install & upgrade integrity (epic) | One trusted install pipeline (addons + helpers): required sha256, entry-validated extract (no unzip for untrusted zips), atomic stage/commit, cancel/crash recovery; then phased layers — `minShellVersion` + universal binary, catalog `dependencies`, upgrade UI, namespaced ids. Long-term L2 + seams for signing/L3. Spec: [2026-09-05 install integrity](architecture/2026-09-05-addon-install-upgrade-integrity-design.md). Plan: [2026-09-05-addon-install-upgrade-integrity](superpowers/plans/2026-09-05-addon-install-upgrade-integrity.md). Absorbs [0018](tickets.md) [0025](tickets.md) [0029](tickets.md) [0031](tickets.md) [0043](tickets.md) when phases Done. | Medium | XL | — | Done | 2026-08-30 | 2026-09-06 | Phases 1–6 shipped (trust/extract → atomic commit → gates → deps → upgrade UI → namespaced ids). Absorbed tickets marked Done. Manual smoke (install/cancel/upgrade/dep disclosure/replace-under-running/namespaced install) still worth a Mac walk. Republish catalog zips under new `jugnu.*` ids on next `addons-v*` release. |
| 0060 | `python-runtime` helper | Pinned standalone CPython helper (not catalog). Spec: [2026-09-04 python-runtime](architecture/2026-09-04-python-runtime-helper-design.md). Plan: [2026-09-04-python-runtime-helper](superpowers/plans/2026-09-04-python-runtime-helper.md). First consumer: clip-tools (0061). | High | M | 0047 | Done | 2026-09-04 | 2026-09-05 | CPython 3.12.14 (PBS 20260901 install_only_stripped); `scripts/fetch-python-runtime.sh` + `package-helper-python-runtime.sh`; zip published on `addons-v1.0.0` (`python-runtime-1.0.0.zip`). |
| 0061 | `clip-tools` Phase 1 | Clipboard text transform commands; declares `python-runtime`. Spec: [2026-09-04 clip-tools](architecture/2026-09-04-clip-tools-design.md). Plan: [2026-09-04-clip-tools](superpowers/plans/2026-09-04-clip-tools.md). No Transform UI (Phase 2 later). | High | L | 0060 | Done | 2026-09-04 | 2026-09-06 | Phase 1 under `addons/jugnu.clip-tools` (49 commands); zip on `addons-v1.0.0`. Id namespaced in 0058 Phase 6 — republish zip with `jugnu.clip-tools` id on next release. Transform panel still Phase 2. |
