# Paste Plain

Replaces the clipboard contents with a plain-text version (via `pbpaste` / `pbcopy`, optionally `textutil`).

```bash
echo '{"api":1,"op":"run","command":"paste","args":{}}' | ./bin/run
```
